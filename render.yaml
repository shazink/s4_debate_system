services:
  - type: web
    name: s4-debate-system
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: cd web && gunicorn api:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: GROQ_API_KEY
        sync: false
